@{
    ViewData["Title"] = "Volunteer hub";
}

<div class="alert alert-warning">
    <h4 class="alert-heading">Volunteer hub</h4>
    <p class="mb-2">Review payment screenshots and approve them so students receive tickets.</p>
    <a asp-controller="Payment" asp-action="Verify" class="btn btn-warning btn-sm text-dark">Verify payments</a>
</div>
